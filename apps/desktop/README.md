# Magical Exchange Desktop

Native JavaFX desktop app placeholder.

The app will later consume the backend API and keep a local offline cache. No desktop UI is implemented in this first backend-focused milestone.
